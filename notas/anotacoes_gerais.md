https://seu-projeto.vercel.app/admin
kanban-pwa-livid.vercel.app/admin

PI

19/03/26
24/04
19/05
19/06

Apresentação

24/06
25/06

Reconhecimento de arquitetura
Algoritmos
Estatística
Banco de Dados


Função/Método,Tipo,Descrição,Exemplo no seu código
len(),Função,Retorna o número de itens (comprimento) de uma lista ou tupla.,len(dataset)
min(),Função,Retorna o menor valor encontrado na lista.,min(dados_limpos)
max(),Função,Retorna o maior valor encontrado na lista.,max(dados_limpos)
sum(),Função,Calcula a soma de todos os itens numéricos da lista.,sum(madrugada)
sorted(),Função,Retorna uma nova lista ordenada (sem alterar a original).,"sorted(dataset, key=...)"
.append(),Método,Adiciona um item ao final da lista existente.,dataset.append(nova_amostra)
.insert(),Método,Insere um item em uma posição (índice) específica.,"dataset.insert(0, [...])"
.pop(),Método,Remove e retorna o último item (ou o item do índice indicado).,dataset.pop(0)
.count(),Método,Conta quantas vezes um valor específico aparece na lista.,"labels.count(""setosa"")"
.index(),Método,Retorna a posição (índice) da primeira ocorrência de um valor.,labels.index('versicolor')
enumerate(),Função,Usada em loops para obter o índice e o valor ao mesmo tempo.,"for i, amostra in enumerate(...)"